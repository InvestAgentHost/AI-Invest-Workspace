# Tools

工具目录只保存代码、测试、模块级说明和示例配置。Workspace 级工具使用指南统一放在 `docs/`。

- `collectors/`：外部平台采集器
- `knowledge/`：清洗、索引和检索工具
- `research/`：公司、行业和数据分析工具
- `shared/`：通用模块

具体工具指南：

- [X Collector 指南](../docs/x-collector.md)：授权浏览器采集、归档、图片关联和本地检索

工具产生的数据应写入顶层 `sources/`、`data/`、`knowledge/` 或 `outputs/`。
