# Data

- `curated/`：小型、稳定、可审阅且可复用的数据，进入 Git。
- `derived/`：可由来源重新生成的数据，Git 忽略。
- `watchlists/`：轻量监控清单，进入 Git。

Gangtise 等外部导出属于原始资料：公司相关数据放在对应的 `sources/companies/<company-id>/`，尚未归属公司的数据才放在 `sources/providers/`。
