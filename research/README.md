# Research

保存经过整理的研究过程和长期结论，包括 `companies/`、`industries/`、`themes/`、`macro/` 与 `sessions/`。

公司目录按投资或交易市场组织为 `companies/<market>/<ticker>-<slug>/`，只保存研究 Markdown、`company.yaml` 和简要的 `materials.md`。用户投递的原件放入对应的 `sources/companies/<market>/<ticker>-<slug>/inbox/`；整理后仍留在该公司原始资料目录，不要求逐文件建索引。
